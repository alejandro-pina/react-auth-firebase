import React from "react";
import Navbar from "../../Navbar/Navbar";
import styles from "./Header.module.css";

const Header = () => (

    <header className={styles.header_default}>
      <Navbar />
    </header>
    
);

export default Header;
